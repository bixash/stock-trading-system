const Positions = () => {
    // const token = read_cookie("token");
  
    // if (!token) {
    //   redirect("auth/login");
    // } else {
      return <div>Positions</div>;
    // }
  };
  
  export default Positions;